import { ProjectStructure } from "../../../common/src/project/structure.js";
/** Generates a functionMetadata.json file from the functions directory. */
export declare const generateFunctionMetadataFile: (projectStructure: ProjectStructure) => Promise<void>;
/** Returns whether or not a functionMetadata.json file should be generated. */
export declare const shouldGenerateFunctionMetadata: (projectStructure: ProjectStructure) => boolean;
