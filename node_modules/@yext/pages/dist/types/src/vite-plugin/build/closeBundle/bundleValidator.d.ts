import { ProjectStructure } from "../../../common/src/project/structure.js";
/**
 * Validates the server-side bundled files.
 */
export declare const validateBundles: (projectStructure: ProjectStructure) => void;
