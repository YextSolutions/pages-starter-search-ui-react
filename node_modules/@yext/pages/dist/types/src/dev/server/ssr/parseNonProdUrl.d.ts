export declare function parseAsEntityUrl(url: URL): {
    feature: string;
    entityId: string;
};
export declare function parseAsStaticUrl(url: URL): {
    staticURL: string;
};
export declare function getLocaleFromUrl(url: URL): string | null;
