interface EntityProfile {
    [field: string]: ProfileValue;
    meta?: {
        accountId: string;
        uid: string;
        id: string;
        timestamp: string;
        folderId: string;
        language: string;
        countryCode: string;
        entityType: string;
    };
    name?: string;
}
type ProfileValue = string | number | boolean | null | undefined | ProfileValue[] | {
    [k: string]: ProfileValue;
};
export declare function buildApiUrl(base: string, path: string, params?: Record<string, string>): string;
export declare function updateEntity<T extends EntityProfile>(id: string, locale: string, body: EntityProfile, apiKey: string, options?: {
    env?: "prod" | "sbx";
    v?: string;
}): Promise<T>;
export {};
