import { globSync } from "glob";
import { convertToPosixPath } from "../../../common/src/template/paths.js";
import path from "path";
const getBundlePaths = (projectStructure) => {
  const { rootFolders, subfolders } = projectStructure.config;
  return globSync(
    convertToPosixPath(
      `${path.resolve(rootFolders.dist, subfolders.assets)}/{${subfolders.renderBundle},${subfolders.renderer},${subfolders.serverBundle},${subfolders.static}}/**/*.*`
    )
  );
};
import { statSync } from "fs";
export {
  getBundlePaths,
  statSync
};
