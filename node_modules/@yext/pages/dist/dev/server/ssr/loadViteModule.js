import { importFresh } from "./moduleImports.js";
const loadViteModule = async (devserver, templateFilepath) => {
  return await importFresh(devserver, templateFilepath);
};
export {
  loadViteModule
};
