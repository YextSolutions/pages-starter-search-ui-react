import { Project } from "ts-morph";
function createTestSourceFile(code, filepath = "test.tsx") {
  const p = createTestProject();
  p.createSourceFile(filepath, code);
  return {
    sourceFile: p.getSourceFileOrThrow(filepath),
    project: p
  };
}
function createTestProject() {
  return new Project({
    compilerOptions: {
      react: "react-jsx"
    }
  });
}
export {
  createTestProject,
  createTestSourceFile as default
};
