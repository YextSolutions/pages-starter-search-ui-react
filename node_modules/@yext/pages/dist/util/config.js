import fs from "node:fs";
import path from "node:path";
const isUsingConfig = (configName, scope) => {
  if (scope) {
    return fs.existsSync(path.resolve(scope, configName));
  }
  return fs.existsSync(configName);
};
export {
  isUsingConfig
};
