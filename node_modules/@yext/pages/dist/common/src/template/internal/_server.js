import * as ReactDOMServer from "react-dom/server";
import * as React from "react";
const render = async (pageContext) => {
  const { Page, pageProps } = pageContext;
  const viewHtml = ReactDOMServer.renderToString(/* @__PURE__ */ React.createElement(Page, { ...pageProps }));
  return `<!DOCTYPE html>
    <html lang="<!--app-lang-->">
      <head></head>
      <body>
        <div id="reactele">${viewHtml}</div>
      </body>
    </html>`;
};
export {
  render
};
