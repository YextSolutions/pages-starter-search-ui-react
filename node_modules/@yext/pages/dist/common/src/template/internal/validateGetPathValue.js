const validateGetPathValue = (value, templateIdentifier) => {
  if (!value || typeof value !== "string") {
    throw new Error(
      `getPath does not return a valid string in template '${templateIdentifier}'`
    );
  }
};
export {
  validateGetPathValue
};
