import * as ReactDOM from "react-dom";
import * as React from "react";
const render = async (pageContext) => {
  const { Page, pageProps } = pageContext;
  ReactDOM.hydrate(
    /* @__PURE__ */ React.createElement(Page, { ...pageProps }),
    document.getElementById("reactele")
  );
};
export {
  render
};
