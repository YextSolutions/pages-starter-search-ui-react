import { pathToFileURL } from "url";
import { import_ } from "./import.js";
const determinePublicFilepath = async (defaultPublicDir, viteConfigPath) => {
  if (viteConfigPath === "") {
    return defaultPublicDir;
  }
  const viteConfig = await import_(pathToFileURL(viteConfigPath).toString());
  const userConfig = viteConfig.default;
  return userConfig.publicDir || defaultPublicDir;
};
export {
  determinePublicFilepath
};
