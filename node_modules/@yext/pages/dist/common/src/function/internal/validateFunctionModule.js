const validateFunctionModule = (filepath, serverlessFunction) => {
  if (!serverlessFunction.default) {
    throw new Error(`${filepath} is missing a default export.`);
  }
};
export {
  validateFunctionModule
};
